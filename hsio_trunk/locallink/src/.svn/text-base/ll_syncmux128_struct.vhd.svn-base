-- VHDL Entity locallink.ll_syncmux128.symbol
--
-- Created by Matt Warren 2014
--
-- Generated by Mentor Graphics' HDL Designer(TM) 2013.1 (Build 6)
--

library ieee;
use ieee.std_logic_1164.all;
use ieee.std_logic_arith.all;
use ieee.std_logic_unsigned.all;
library utils;
use utils.pkg_types.all;
library hsio;
use hsio.pkg_hsio_globals.all;

entity ll_syncmux128 is
   port( 
      clk            : in     std_logic;
      lld_i          : in     std_logic;
      rst            : in     std_logic;
      s_lls_i        : in     t_llsrc_array (127 downto 0);
      lls_o          : out    t_llsrc;
      s_lld_o        : out    std_logic_vector (127 downto 0);
      selected_str_o : out    std_logic_vector (6 downto 0)
   );

-- Declarations

end ll_syncmux128 ;

-- VHDL from Block Diagram 
-- Generated by Mentor Graphics HDL Designer(TM) 2013.1 (Build 6) 
--
-- locallink.ll_syncmux128.struct
--
-- Created by Matt Warren 2014
--

library ieee;
use ieee.std_logic_1164.all;
use ieee.std_logic_arith.all;
use ieee.std_logic_unsigned.all;

library utils;
use utils.pkg_types.all;
library hsio;
use hsio.pkg_hsio_globals.all;


architecture struct of ll_syncmux128 is

   -- Architecture declarations

   -- Internal signal declarations
   signal freeze : std_logic;
   signal sel    : std_logic_vector(6 downto 0);


   -- Component Declarations
   component ll_mux_ctrl128
   port (
      clk       : in     std_logic;
      dst_rdy_i : in     std_logic;
      rst       : in     std_logic;
      s_lls_i   : in     t_llsrc_array (127 downto 0);
      freeze_o  : out    std_logic;
      s_lldr_o  : out    std_logic_vector (127 downto 0);
      sel_o     : out    std_logic_vector (6 downto 0)
   );
   end component;
   component ll_smx128
   port (
      lls_o    : out    t_llsrc ;
      s_lls_i  : in     t_llsrc_array (127 downto 0);
      freeze_i : in     std_logic ;
      sel_i    : in     std_logic_vector (6 downto 0);
      clk      : in     std_logic ;
      rst      : in     std_logic 
   );
   end component;


begin

   -- ModuleWare code(v1.12) for instance 'U_0' of 'buff'
   selected_str_o <= sel;

   -- Instance port mappings.
   Umuxctrl : ll_mux_ctrl128
      port map (
         s_lls_i   => s_lls_i,
         s_lldr_o  => s_lld_o,
         dst_rdy_i => lld_i,
         freeze_o  => freeze,
         sel_o     => sel,
         rst       => rst,
         clk       => clk
      );
   Usmx128 : ll_smx128
      port map (
         lls_o    => lls_o,
         s_lls_i  => s_lls_i,
         freeze_i => freeze,
         sel_i    => sel,
         clk      => clk,
         rst      => rst
      );

end struct;
