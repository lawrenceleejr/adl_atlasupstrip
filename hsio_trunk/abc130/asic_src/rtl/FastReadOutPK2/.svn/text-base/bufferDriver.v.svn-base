`timescale 1ns/1ps
/******************
Drive the serializer output with a buffer
************/
module bufferDriver (in, out);

input in;

output out;

 // ***BUFFER_O driver(.A(in),.Z(out),.VDD(),.GND(),.NW(),.SX());
//BUFFER_O driver(.A(in),.Z(out), .VDD(VDD), .GND(GND), .NW(NW), .SX(SX) );

assign out = in; // ***

endmodule
